
===== TRIAGE 2026-09-09 15:21:03 UTC =====
[0m
> build · mimo-v2.5-free
[0m
[0m→ [0mRead .
[0m→ [0mRead new-leads.txt
[0m→ [0mRead probe-results.md
[0m→ [0mRead scope.yml
[0m→ [0mRead leads
[0m→ [0mRead triage
[0m→ [0mRead inventory
[0m→ [0mRead leads/lead-mimo.md
[0m→ [0mRead inventory/sumup.md
[0m→ [0mRead leads/lead-bigpickle.md
[0m→ [0mRead leads/lead-nemotron3.md
[0m→ [0mRead leads/lead-longcat.md
[0m→ [0mRead leads/lead-ling3.md
[0m→ [0mRead leads/lead-laguna.md
[0m→ [0mRead triage/run-2026-09-09-11-38.md
[0m→ [0mRead leads/reposcan-sumup.md
Let me read the latest triage run and the reposcan lead file for the most actionable leads.
[0m→ [0mRead triage/run-2026-09-09-06-06.md
[0m→ [0mRead triage/run-2026-09-09-01-09.md
[0m→ [0mRead triage/run-2026-09-08-22-47.md
The `LEADS:` section in your message is empty — no leads were pasted after the label. This matches the pattern from the last ~10 triage runs which all returned empty-lead responses.

However, I can see actionable findings accumulated in `probe-results.md` and the inventory. Let me extract and validate the top candidates from the probe data:

---

## Extracted Leads from Probe Results — 7-Question Gate

### LEAD 1: `help.sumup.com` Contentful Preview API Token Leak
| # | Question | Answer |
|---|----------|--------|
| Q1 | In scope? | Yes — `*.sumup.com` domain, SumUp-owned help center infrastructure |
| Q2 | Attacker reachable? | Yes — anonymous HTTP GET to `https://help.sumup.com` returns 200 with token in page source |
| Q3 | Real security impact? | Yes — unauthenticated read of 1,082 draft/unpublished entries (incl. 102 articles) in Contentful space `214q1nptnllb`; premature disclosure of unreleased features/roadmap |
| Q4 | Provable non-invasively? | Yes — `curl https://help.sumup.com` leaks the Contentful Preview API token in the HTML; `curl 'https://preview.contentful.com/spaces/214q1nptnllb/entries?access_token=<TOKEN>'` returns draft entries. Read-only, no auth required. |
| Q5 | Novel/unreported? | Yes — inventory marks as "CONFIRMED reportable (P4, conf 92) — report still unfiled" |
| Q6 | Not on always-rejected list? | Correct — not DoS, not header leak, not known-vuln, not clickjacking |
| Q7 | Would reasonable triager accept? | Yes — genuine misconfig with concrete POC |

**Verdict: VALID**
- **Impact:** P4 — Information disclosure (unpublished content leak via misconfigured API token)
- **CVSS 3.1:** 5.3 (Medium) — AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N
- **Proof:** `curl https://help.sumup.com` → extract Contentful token from page source → `curl 'https://preview.contentful.com/spaces/214q1nptnllb/entries?access_token=<TOKEN>'` → returns 1,082 draft entries
- **Channel:** bugs.olivermaicher.eu

---

### LEAD 2: `auth.sam-app.ro` — Public Staging Dynamic Client Registration + JWT Minting
| # | Question | Answer |
|---|----------|--------|
| Q1 | In scope? | **HOLD** — `sam-app.ro` is a staging environment; scope says "All company-owned infrastructure" but staging domains are ambiguous — needs clarification |
| Q2 | Attacker reachable? | Yes — POST to `https://auth.sam-app.ro/oauth2/register` returns 201 with client_id+secret+chosen redirect_uris (unauthenticated RFC 7591) |
| Q3 | Real security impact? | Yes — dynamic clients can mint real JWT access_tokens via `client_credentials` grant with attacker-controlled `aud`; cross-env key isolation confirmed but staging tokens could be used if staging shares prod data |
| Q4 | Provable non-invasively? | Yes — POST to registration endpoint (creates client, read-only side-effect); POST to token endpoint (mints JWT). No destructive action. |
| Q5 | Novel/unreported? | Yes — staging/prod divergence confirmed; no dynamic registration in prod `auth.sumup.com` |
| Q6 | Not on always-rejected list? | Correct |
| Q7 | Would reasonable triager accept? | **Depends** — if staging is in scope, yes (high); if staging is explicitly out of scope, no |

**Verdict: HOLD** — Scope ambiguity on `sam-app.ro`. If in scope, this is HIGH severity (CVSS 3.1: 8.1) — attacker-controlled `aud` in minted JWTs could pivot to other staging services (`api.sam-app.ro`, `mcp.sam-app.ro`).

---

### LEAD 3: `api.sumup.com/authorize` — Client ID Oracle + Wildcard CORS + Legacy Redirect_uri Divergence
| # | Question | Answer |
|---|----------|--------|
| Q1 | In scope? | Yes — `api.sumup.com` is core SumUp API infrastructure |
| Q2 | Attacker reachable? | Yes — GET returns 302 → auth error flow with distinct error taxonomy |
| Q3 | Real security impact? | Low — error taxonomy (`invalid_client` vs `invalid_request`) is a standard OAuth error pattern; wildcard CORS on an authorize endpoint that only redirects to error pages has limited exploitability without a valid registered client |
| Q4 | Provable non-invasively? | Yes — GET requests only |
| Q5 | Novel/unreported? | The legacy gateway divergence (dashboard client accepted on auth.sumup.com but rejected on api.sumup.com) is interesting but the callback host enumeration yielded 0 hits across 163 combos |
| Q6 | Not on always-rejected list? | Correct — not a header leak or descriptive error |
| Q7 | Would reasonable triager accept? | **No** — error taxonomy on OAuth authorize endpoints is informational; wildcard CORS without demonstrated credential theft is a hardening suggestion, not a vulnerability. Callback host exhaustive sweep = 0 hits. |

**Verdict: INVALID** — Informational at best. Error taxonomy is standard OAuth behavior; CORS on an authorize endpoint that doesn't set cookies or tokens is low-risk. No concrete exploit path demonstrated.

---

### LEAD 4: `mcp.sumup.com` Wildcard CORS
| # | Question | Answer |
|---|----------|--------|
| Q1 | In scope? | Yes |
| Q2 | Attacker reachable? | Yes — OPTIONS returns wildcard CORS |
| Q3 | Real security impact? | Low — endpoint requires Bearer JWT; wildcard CORS with Authorization header-only (no cookies) means browser won't auto-send creds cross-origin unless explicitly coded |
| Q4 | Provable non-invasively? | Yes |
| Q5 | Novel? | Already rejected by prior triage as "hardening-only" |
| Q6 | Not on rejected list? | Correct |
| Q7 | Accept? | **No** — already rejected; Bearer-only auth with CORS `*` is a standard pattern, not a misconfig |

**Verdict: INVALID** — Already rejected. Hardening suggestion, not a vulnerability.

---

## Summary

| # | Lead | Verdict | CVSS | Priority |
|---|------|---------|------|----------|
| 1 | help.sumup.com Contentful Preview Token Leak | **VALID** | 5.3 (Medium) | **FILE REPORT** |
| 2 | auth.sam-app.ro Staging Dynamic Registration | **HOLD** | 8.1 if in scope | Clarify scope first |
| 3 | api.sumup.com/authorize Client ID Oracle | **INVALID** | — | Informational |
| 4 | mcp.sumup.com Wildcard CORS | **INVALID** | — | Already rejected |

**Recommended action:** File Lead 1 (help.sumup.com) immediately — concrete POC, no scope ambiguity, genuine misconfig. Hold Lead 2 pending scope clarification on `sam-app.ro`.
