
===== TRIAGE 2026-09-10 19:11:04 UTC =====
[0m
> build · mimo-v2.5-free
[0m
[0m→ [0mRead .
[0m→ [0mRead leads
[0m→ [0mRead probe-results.md
[0m→ [0mRead scope.yml
[0m→ [0mRead leads/lead-bigpickle.md
[0m→ [0mRead leads/lead-laguna.md
[0m→ [0mRead leads/lead-ling3.md
[0m→ [0mRead leads/lead-longcat.md
[0m→ [0mRead leads/lead-mimo.md
[0m→ [0mRead leads/lead-nemotron3.md
[0m→ [0mRead leads/lead-nemotron3.md[90m [offset=393][0m
[0m→ [0mRead leads/reposcan-sumup.md
[0m→ [0mRead new-leads.txt
[0m→ [0mRead triage-output.txt
No leads were provided in the message. I've reviewed all lead files and probe results. Let me triage the distinct findings that emerged from the hunt bots.

---

## TRIAGE OUTPUT — SumUp Payments Bug Bounty

### LEAD 1: `portal.sumup.com` — Live CRM Login Page (React/iriscrm.com)

| Question | Answer |
|----------|--------|
| Q1 Scope | YES — portal.sumup.com is SumUp-operated merchant portal |
| Q2 Reachable | YES — returns HTTP 200 with React login page |
| Q3 Impact | LOW — login page disclosure only; no parameter or endpoint discovered for SSRF |
| Q4 Provable | YES — `GET https://portal.sumup.com/` returns 200 |
| Q5 Novel | NO — CNAME to iriscrm.com is known; no exploitable surface found |
| Q6 Not always-rejected | YES — not on always-rejected list |
| Q7 Reasonable triager | NO — informational disclosure only, no security impact |

**Verdict: INVALID** — Login page exposure on a third-party CRM (iriscrm.com) is informational only. No exploitable parameters discovered.

---

### LEAD 2: `help.sumup.com/api/search/articles` — HTTP 500

| Question | Answer |
|----------|--------|
| Q1 Scope | YES — help.sumup.com is SumUp-operated |
| Q2 Reachable | YES — returns HTTP 500 |
| Q3 Impact | NONE — 500 on search API is normal error handling |
| Q4 Provable | YES — probe confirmed 500 |
| Q5 Novel | NO — search endpoint errors are standard behavior |
| Q6 Not always-rejected | YES |
| Q7 Reasonable triager | NO — a 500 error on a search endpoint is not a vulnerability |

**Verdict: INVALID** — HTTP 500 on a search API is normal server behavior, not a security issue.

---

### LEAD 3: `mcp.sumup.com/mcp` — HTTP 401

| Question | Answer |
|----------|--------|
| Q1 Scope | YES — mcp.sumup.com is SumUp-operated |
| Q2 Reachable | YES — returns HTTP 401 (requires auth) |
| Q3 Impact | NONE — proper 401 enforcement means auth is working correctly |
| Q4 Provable | YES — `GET https://mcp.sumup.com/mcp` returns 401 |
| Q5 Novel | NO — authenticated endpoint returning 401 is expected |
| Q6 Not always-rejected | YES |
| Q7 Reasonable triager | NO — correct auth enforcement is not a vulnerability |

**Verdict: INVALID** — HTTP 401 on an authenticated endpoint demonstrates proper access control.

---

### LEAD 4: `auth.sumup.com` — OIDC Discovery Fully Exposed

| Question | Answer |
|----------|--------|
| Q1 Scope | YES — auth.sumup.com is in-scope |
| Q2 Reachable | YES — `.well-known/openid-configuration` returns 200 |
| Q3 Impact | NONE — OIDC discovery documents are public by design per RFC 8414 |
| Q4 Provable | YES — `GET https://auth.sumup.com/.well-known/openid-configuration` returns 200 |
| Q5 Novel | NO — OIDC discovery is intentionally public |
| Q6 Not always-rejected | YES — but public OIDC docs are informational |
| Q7 Reasonable triager | NO — this is standard OAuth2/OIDC behavior, not a vulnerability |

**Verdict: INVALID** — OIDC discovery endpoints are public by specification. No security impact.

---

### LEAD 5: `auth.sumup.com` — `token_endpoint_auth_methods=none` (Public Client Impersonation)

| Question | Answer |
|----------|--------|
| Q1 Scope | YES |
| Q2 Reachable | YES — documented in OIDC discovery |
| Q3 Impact | POTENTIALLY CRITICAL — allows token requests without client_secret |
| Q4 Provable | NO — PAR and token endpoints require client authentication; dashboard client rejects unauthenticated requests |
| Q5 Novel | PARTIALLY — "none" is documented but restricted to specific client configs |
| Q6 Not always-rejected | YES |
| Q7 Reasonable triager | NO — configuration exists but is not exploitable without a registered client |

**Verdict: HOLD** — Interesting OAuth configuration, but requires AUTH_HELPED to verify. Dashboard client rejects unauthenticated token requests. Low current yield without merchant OAuth token.

---

### LEAD 6: `api.sumup.com/authorize` — Legacy OAuth Client ID Oracle

| Question | Answer |
|----------|--------|
| Q1 Scope | YES — api.sumup.com is in-scope |
| Q2 Reachable | YES — returns 302→error flow with different errors for valid/invalid client_ids |
| Q3 Impact | MEDIUM — client_id oracle enables enumeration of registered OAuth clients |
| Q4 Provable | YES — `GET https://api.sumup.com/authorize?client_id=dashboard` returns invalid_request; unknown IDs return invalid_client |
| Q5 Novel | YES — legacy authorize endpoint on API gateway (distinct from auth.sumup.com) is newly discovered |
| Q6 Not always-rejected | YES |
| Q7 Reasonable triager | YES — client_id enumeration via error differentiation is a valid finding |

**Verdict: VALID** — Client ID oracle on legacy OAuth endpoint. Read-only proof: GET requests to `/authorize` with different client_ids show error differentiation. Impact: Medium (information disclosure enabling OAuth client enumeration). CVSS 3.1: 5.3 (AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N). Report via bugs.olivermaicher.eu.

---

### LEAD 7: `api.sumup.com/authorize` — Wildcard CORS + SameSite=None Cookies

| Question | Answer |
|----------|--------|
| Q1 Scope | YES |
| Q2 Reachable | YES — response includes `Access-Control-Allow-Origin: *` and `SameSite=None` cookies |
| Q3 Impact | MEDIUM — wildcard CORS on OAuth authorize endpoint could enable CSRF-like token interaction |
| Q4 Provable | YES — OPTIONS/HEAD reveals CORS headers |
| Q5 Novel | YES — endpoint-specific CORS divergence from auth.sumup.com |
| Q6 Not always-rejected | YES |
| Q7 Reasonable triager | MAYBE — wildcard CORS on an OAuth authorize endpoint is unusual but the endpoint already requires authentication |

**Verdict: HOLD** — Interesting misconfiguration, but the authorize endpoint already requires user interaction (login). Low practical impact without additional vulnerabilities. Needs further analysis of whether credentials are actually sent cross-origin.

---

### LEAD 8: `admin.sumup.com` — Authentication Bypass via Header Spoofing

| Question | Answer |
|----------|--------|
| Q1 Scope | YES |
| Q2 Reachable | YES — returns 403 |
| Q3 Impact | CRITICAL if successful |
| Q4 Provable | NO — all header spoofing attempts (Host, X-Forwarded-For, X-Original-URL) return identical 403 |
| Q5 Novel | NO — header spoofing is a well-known technique |
| Q6 Not always-rejected | YES |
| Q7 Reasonable triager | NO — no evidence of bypass; identical 403 responses |

**Verdict: INVALID** — No auth bypass demonstrated. Header spoofing yields identical 403 responses. Server correctly rejects manipulated headers.

---

### LEAD 9: `me.sumup.com` — Vercel Surface / OAuth2 App Registration Logic

| Question | Answer |
|----------|--------|
| Q1 Scope | YES — me.sumup.com is SumUp-operated merchant self-service |
| Q2 Reachable | YES — returns 307/403 on anonymous requests |
| Q3 Impact | HIGH if misconfigured |
| Q4 Provable | NO — all anonymous /api/* routes redirect to OAuth or return 403; requires authenticated session |
| Q5 Novel | YES — distinct Vercel-served asset (non-Cloudflare) |
| Q6 Not always-rejected | YES |
| Q7 Reasonable triager | NO — no misconfiguration found passively; requires AUTH_HELPED |

**Verdict: HOLD** — Interesting new attack surface (Vercel vs Cloudflare edge), but no passive findings. Requires authenticated session to enumerate. Low yield without merchant token.

---

### LEAD 10: `api.sumup.com` — BOLA via Dashboard-Client Scopes

| Question | Answer |
|----------|--------|
| Q1 Scope | YES |
| Q2 Reachable | YES — but all unauthenticated paths return 404 |
| Q3 Impact | CRITICAL — cross-merchant financial/PII data exposure |
| Q4 Provable | NO — requires valid merchant OAuth token (AUTH_HELPED) |
| Q5 Novel | NO — BOLA/IDOR is a well-known class |
| Q6 Not always-rejected | YES |
| Q7 Reasonable triager | NO — no proof of vulnerability without merchant token |

**Verdict: HOLD** — High-impact class but requires AUTH_HELPED to verify. Scope catalog from OIDC discovery maps the API surface, but no cross-tenant access demonstrated.

---

### LEAD 11: Reposcan — Cloudflare Account ID in `wrangler.jsonc`

| Question | Answer |
|----------|--------|
| Q1 Scope | YES — sumup-mcp is SumUp code |
| Q2 Reachable | YES — public GitHub repo |
| Q3 Impact | LOW — account IDs are not full secrets; aids reconnaissance |
| Q4 Provable | YES — `wrangler.jsonc` contains `2037fc18a2fb8175c20d20776cac65c5` |
| Q5 Novel | NO — Cloudflare account IDs are semi-public |
| Q6 Not always-rejected | YES |
| Q7 Reasonable triager | NO — CF account IDs are not credentials; program explicitly excludes "exposure of known public files" and informational disclosure |

**Verdict: INVALID** — Cloudflare account IDs are not secrets. Exclusion: "Descriptive error messages or headers (e.g. Stack Traces, banner grabbing)" and information disclosure.

---

### LEAD 12: Reposcan — Internal Staging Domains Exposed (`sam-app.ro`)

| Question | Answer |
|----------|--------|
| Q1 Scope | YES — internal infrastructure |
| Q2 Reachable | MAYBE — DNS resolution unconfirmed from public internet |
| Q3 Impact | MEDIUM — exposes internal naming convention; potential attack surface |
| Q4 Provable | PARTIALLY — can verify DNS resolution passively |
| Q5 Novel | YES — internal domains in public repo |
| Q6 Not always-rejected | YES |
| Q7 Reasonable triager | MAYBE — informational; depends on whether hosts resolve publicly |

**Verdict: HOLD** — Interesting info disclosure, but impact depends on DNS resolution. Verify passively: `dig sam-app.ro`, `dig api.sam-app.ro`. If they resolve to internal IPs or don't resolve, impact is negligible.

---

### LEAD 13: Reposcan — Wildcard CORS on `mcp.sumup.com`

| Question | Answer |
|----------|--------|
| Q1 Scope | YES — mcp.sumup.com is SumUp-operated |
| Q2 Reachable | YES — mcp.sumup.com/mcp returns 401 |
| Q3 Impact | MEDIUM — wildcard CORS on OAuth-authenticated endpoint |
| Q4 Provable | PARTIALLY — can verify with OPTIONS request |
| Q5 Novel | YES — endpoint-specific misconfiguration |
| Q6 Not always-rejected | YES |
| Q7 Reasonable triager | MAYBE — wildcard CORS on an auth-protected payment MCP server is unusual |

**Verdict: HOLD** — Wildcard CORS on an authenticated endpoint is a misconfiguration, but requires verifying if credentials are actually sent cross-origin in browser context. Needs active verification (OPTIONS request with Origin header).

---

### LEAD 14: Reposcan — Vendure Webhook Missing Signature Verification

| Question | Answer |
|----------|--------|
| Q1 Scope | YES — sumup-plugin-vendure is SumUp code |
| Q2 Reachable | NO — webhook endpoint is in plugin code, not on live SumUp infrastructure |
| Q3 Impact | MEDIUM — payment state manipulation if deployed |
| Q4 Provable | YES — source code review shows no HMAC verification |
| Q5 Novel | YES — contrasts with ACP repo which implements proper verification |
| Q6 Not always-rejected | YES |
| Q7 Reasonable triager | NO — this is a code quality issue in an open-source plugin, not a live vulnerability on SumUp infrastructure |

**Verdict: INVALID** — Missing webhook verification in an open-source plugin is a code quality issue, not a vulnerability on SumUp's live infrastructure. The plugin is designed for integrators to add their own verification.

---

### LEAD 15: Reposcan — Source Maps Enabled on Cloudflare Workers

| Question | Answer |
|----------|--------|
| Q1 Scope | YES |
| Q2 Reachable | MAYBE — requires accessing Workers runtime |
| Q3 Impact | LOW — source code exposure via source maps |
| Q4 Provable | PARTIALLY — can check for source map references in responses |
| Q5 Novel | NO — source map exposure is a well-known class |
| Q6 Not always-rejected | YES |
| Q7 Reasonable triager | NO — program explicitly excludes "Descriptive error messages or headers" and "Outdated software / library versions" |

**Verdict: INVALID** — Source maps are a deployment configuration issue. Program excludes information disclosure and descriptive errors.

---

## SUMMARY

| Verdict | Count | Leads |
|---------|-------|-------|
| VALID | 1 | #6: Legacy OAuth client_id oracle on api.sumup.com/authorize |
| HOLD | 5 | #5 (token_auth_methods=none), #7 (wildcard CORS), #9 (me.sumup.com), #10 (BOLA), #12 (staging domains), #13 (MCP CORS) |
| INVALID | 9 | #1, #2, #3, #4, #8, #11, #14, #15 |

**Recommended next actions:**
1. **REPORT #6** — Legacy OAuth client_id oracle is provable, novel, and has clear impact
2. **INVESTIGATE #5, #7, #13** — Require active verification (AUTH_HELPED or OPTIONS requests)
3. **MONITOR #9, #10** — Require merchant OAuth token to test
4. **PASS on all INVALID** — Informational-only or out-of-scope findings
