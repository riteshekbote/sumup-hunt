
===== TRIAGE 2026-09-12 00:39:58 UTC =====
[0m
> build · mimo-v2.5-free
[0m
[0m✱ [0mGlob "**/*sumup*"[90m 2 matches[0m
[0m✱ [0mGlob "**/*scope*"[90m 1 match[0m
[0m✱ [0mGlob "**/*lead*"[90m 7 matches[0m
[0m✱ [0mGlob "**/*.yml"[90m 1 match[0m
[0m✱ [0mGlob "**/*.yaml"[90m 0 matches[0m
I see the leads section is empty. Let me check if there are lead files or scope definitions in the workspace that I should be referencing.
[0m→ [0mRead scope.yml
[0m→ [0mRead inventory/sumup.md
[0m→ [0mRead new-leads.txt
[0m→ [0mRead leads/lead-bigpickle.md
[0m→ [0mRead leads/lead-nemotron3.md
[0m→ [0mRead leads/lead-longcat.md
[0m→ [0mRead leads/lead-ling3.md
[0m→ [0mRead leads/lead-laguna.md
[0m→ [0mRead leads/reposcan-sumup.md
Let me read the hunt bot lead files to see what was produced.
[0m→ [0mRead inventory/sumup.md[90m [offset=412][0m
[0m→ [0mRead leads/lead-bigpickle.md[90m [offset=437][0m
Now let me read the rest of the inventory to see the full scope of confirmed findings.
The leads section in your message was empty, but I've read all the hunt bot lead files and the inventory. Let me validate the **distinct, non-trivial leads** that emerged from the bots against the 7-Question Gate.

---

## LEAD 1: help.sumup.com Contentful Preview API Token Leak

**Source:** inventory (confirmed 2026-09-08, token rotated 2026-09-11)

| # | Question | Answer |
|---|----------|--------|
| Q1 In scope? | **YES** — `help.sumup.com` is a SumUp-operated domain per scope.yml ("all company-owned... websites, and domains") |
| Q2 Attacker reachable? | **YES** — Token exposed in client-side JS (`_app-*.js`), fully unauthenticated GET to `preview.contentful.com/spaces/214q1nptnllb` |
| Q3 Real impact? | **YES** — Read access to 1,082 draft/unpublished entries (incl. 102 articles) + 62 unpublished assets in Contentful CMS; internal naming exposed ("Payouts test") |
| Q4 Provable non-invasively? | **YES** — Single GET request with leaked token; no writes attempted; read-only |
| Q5 Novel/unreported? | **YES** — No prior report found; token was live 2026-09-08 through 2026-09-11 |
| Q6 Not always-rejected? | **YES** — Not DoS, not brute-force, not banner grab, not known-vulnerable lib, not CSRF-on-logout |
| Q7 Reasonable triager? | **YES** — Client-side secret leak granting unauthenticated CMS read is a standard bounty class |

**Verdict: VALID** (finding now closed — token rotated 2026-09-11)

- **Proof steps:** `GET https://preview.contentful.com/spaces/214q1nptnllb/entries?access_token=XRP4rB5w...` — returns JSON with draft entries
- **Impact:** Low-Medium (information disclosure of unpublished content; no PII/payment data confirmed in CMS entries)
- **CVSS 3.1:** 5.3 (AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N)
- **Channel:** bugs.olivermaicher.eu (private program)

---

## LEAD 2: auth.sumup.com `token_endpoint_auth_methods=["none"]` OIDC Discovery Disclosure

**Source:** bigpickle (conf 70), nemotron3 (conf 70)

| # | Question | Answer |
|---|----------|--------|
| Q1 In scope? | **YES** — `auth.sumup.com` is SumUp's auth server |
| Q2 Attacker reachable? | **YES** — `.well-known/openid-configuration` returns 200 unauthenticated |
| Q3 Real impact? | **NO** — `token_endpoint_auth_methods_supported=["none"]` is declared in OIDC discovery (RFC 6749 §2 / OIDC Core §8.1) but the actual `dashboard` client rejects unauthenticated token requests. PAR/device routes return 404 on OPTIONS (unrouted). The declaration is informational metadata, not a misconfiguration — the server enforces auth at the token endpoint. |
| Q4 Provable? | Metadata is observable, but no exploit path exists without a bypass |
| Q5 Novel? | No — standard OIDC metadata; "none" auth method is a common declaration for public clients |
| Q6 Not always-rejected? | Would fall under "SSL/TLS best practices" / informational if no exploit |
| Q7 Reasonable triager? | **NO** — Declaration of supported methods ≠ vulnerability. Server correctly rejects unauthenticated requests. |

**Verdict: INVALID** — OIDC metadata disclosure of `token_endpoint_auth_methods` is informational, not a vulnerability. Server-side enforcement is confirmed (401/400 on unauthenticated token requests).

---

## LEAD 3: auth.sumup.com `request_object_signing_alg_values_supported=["none"]`

**Source:** bigpickle (conf 42), nemotron3

| # | Question | Answer |
|---|----------|--------|
| Q1 In scope? | **YES** |
| Q2 Attacker reachable? | **YES** — OIDC discovery accessible |
| Q3 Real impact? | **NO** — `alg=none` in discovery metadata is per-spec for public clients; `redirect_uri` is strictly allowlisted per client (confirmed); no flow accepts unsigned request objects in testing (GET /oauth2/auth with alg=none returns 405) |
| Q4 Provable? | Observable metadata only; no exploit chain demonstrated |
| Q5 Novel? | No — standard OIDC metadata field |
| Q7 Reasonable triager? | **NO** — Same reasoning as Lead 2; metadata ≠ misconfiguration |

**Verdict: INVALID** — Metadata-only. No exploitable path exists given strict redirect_uri allowlisting.

---

## LEAD 4: api.sumup.com/authorize Wildcard CORS + Client ID Oracle

**Source:** bigpickle (conf 45-55), nemotron3

| # | Question | Answer |
|---|----------|--------|
| Q1 In scope? | **YES** |
| Q2 Attacker reachable? | **YES** — `/authorize` returns 302 unauthenticated |
| Q3 Real impact? | **NO** — Wildcard CORS on an OAuth error redirect (no code/state ever returned to attacker) is explicitly out-of-scope per program: "Cookies that lack HTTP Only or Secure settings for non-sensitive data" and "Clickjacking, without additional details demonstrating a specific exploit". Client ID enumeration via error taxonomy (invalid_client vs invalid_request) is informational. |
| Q4 Provable? | Observable but no data exposure |
| Q5 Novel? | No — CORS wildcard on OAuth endpoints is a common pattern |
| Q6 Not always-rejected? | Wildcard CORS standalone is out-of-scope class per program |
| Q7 Reasonable triager? | **NO** — No code/state/data demonstrated in CORS-readable response; callback host enumeration fully exhausted (~200 combos, 0 HITs) |

**Verdict: INVALID** — Standalone wildcard CORS on OAuth error redirects is out-of-scope. Client ID oracle is informational enumeration only.

---

## LEAD 5: web.sumup.com Subdomain Takeover Candidate

**Source:** bigpickle (conf 45)

| # | Question | Answer |
|---|----------|--------|
| Q1 In scope? | **YES** — `web.sumup.com` is a SumUp domain |
| Q2 Attacker reachable? | **YES** — DNS resolves to 77.246.42.130 (Rackspace UK-RACKSPACE-20070509); host is non-responsive on :80/:443 |
| Q3 Real impact? | **YES (conditional)** — If claimed: full content hosting under `sumup.com` brand (phishing, drive-by, cookie theft for `Domain=sumup.com` cookies shared with dev/app assets) |
| Q4 Provable non-invasively? | **PARTIAL** — RDAP confirms third-party IP (Rackspace, not SumUp-owned). But no claimable service state observed (host fully non-responsive). PoC requires either (a) observing a future change or (b) interacting with Rackspace to claim — touching third-party infra. |
| Q5 Novel/unreported? | **YES** — No prior report found |
| Q6 Not always-rejected? | **YES** — Subdomain takeover is a standard bounty class |
| Q7 Reasonable triager? | **HOLD** — Classic takeover pattern (dedicated A record to third-party pool, no service) but dormant: no claimable state exists yet. Would accept only when a service appears or claim path is confirmed. |

**Verdict: HOLD** — Real pattern but dormant. Needs re-probing to confirm a claimable state exists before reporting. Parked in bot logs.

---

## Summary

| # | Lead | Verdict | Action |
|---|------|---------|--------|
| 1 | Contentful Preview Token Leak (help.sumup.com) | **VALID** (closed) | Report if still live at time of submission; token rotated |
| 2 | `token_endpoint_auth_methods=["none"]` metadata | **INVALID** | Server enforces auth; metadata ≠ vuln |
| 3 | `request_object alg=none` metadata | **INVALID** | Metadata-only; strict allowlist blocks exploit |
| 4 | Wildcard CORS + client_id oracle (api.sumup.com/authorize) | **INVALID** | Out-of-scope class; no data exposure |
| 5 | web.sumup.com subdomain takeover | **HOLD** | Dormant; re-probe nightly for claimable state |

**Recommended next action:** The Contentful token leak (Lead 1) was the only VALID finding but is now closed. All remaining high-impact leads (BOLA, OAuth app registration, PAR flow) require AUTH_HELPED merchant sessions. The web.sumup.com takeover (Lead 5) is the only passive lead worth monitoring.
