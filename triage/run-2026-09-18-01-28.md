
===== TRIAGE 2026-09-18 01:28:35 UTC =====
[0m
> build · mimo-v2.5-free
[0m
[0m✱ [0mGlob "**/*.yml"[90m 1 match[0m
[0m✱ [0mGlob "**/sumup*"[90m 1 match[0m
[0m✱ [0mGlob "**/leads*"[90m 0 matches[0m
I'll help you validate leads against the 7-Question Gate. First, I need to check for the program scope and any leads that were produced.
[0m→ [0mRead scope.yml
[0m→ [0mRead inventory/sumup.md
[0m→ [0mRead inventory/sumup.md[90m [offset=412][0m
[0m✱ [0mGlob "**/*lead*"[90m 7 matches[0m
[0m✱ [0mGlob "**/*probe*"[90m 2 matches[0m
[0m✱ [0mGlob "**/*finding*"[90m 1 match[0m
[0m✱ [0mGlob "**/*valid*"[90m 1 match[0m
[0m→ [0mRead new-leads.txt
[0m→ [0mRead probe-results.txt
[0m→ [0mRead leads/lead-mimo.md
[0m→ [0mRead leads/lead-bigpickle.md
[0m→ [0mRead leads/lead-nemotron3.md
[0m→ [0mRead leads/lead-longcat.md
[0m→ [0mRead leads/lead-ling3.md
[0m→ [0mRead leads/lead-laguna.md
[0m→ [0mRead reports/valid-bugs.md
**No leads provided after "LEADS:"** — the message is empty at that point. I've reviewed the full codebase. Let me validate the distinct hypothesis classes surfaced by the hunt bot against the 7-Question Gate.

---

## 7-Question Gate Validation

### Lead 1: `auth.sam-app.ro` unauthenticated RFC 7591 dynamic client registration → mintable JWTs

| # | Question | Answer |
|---|----------|--------|
| Q1 | In scope? | **YES** — `sam-app.ro` is SumUp staging infra (mcp-theta/api-theta/auth-theta replicate prod gates). Scope covers "all company-owned infrastructure." |
| Q2 | Attacker reachable? | **YES** — unauthenticated POST to `/oauth2/register` returns 201 with `client_id`+`client_secret`; no auth required. |
| Q3 | Real impact? | **YES** — attacker-controlled OAuth clients mint real JWT access tokens via `client_credentials` grant. Tokens valid at `api.sam-app.ro` gateway (structured error response). Staging environments are weaker than prod; credential reuse or pivot to prod is plausible. |
| Q4 | Provable non-invasively? | **YES** — single POST with curl, returns JSON with credentials + tokens. Read-only proof: GET `.well-known/openid-configuration` confirms registration_endpoint; POST with minimal JSON body yields 201. |
| Q5 | Novel/unreported? | **YES** — not in `valid-bugs.md` as filed report. |
| Q6 | On always-rejected list? | **NO** — not DoS, not informational banner leak, not public file disclosure. |
| Q7 | Reasonable triager? | **YES** — unauthenticated client registration on staging auth server with mintable tokens is a valid finding. |

**Verdict: VALID**
**Impact:** HIGH (7.5) — staging auth → real JWTs, staging is weaker control plane.
**Proof:** `POST https://auth.sam-app.ro/oauth2/register` → 201 `{"client_id":"...","client_secret":"..."}` then `POST /oauth2/token` → 200 `{"access_token":"..."}`.
**Channel:** bugs.olivermaicher.eu

---

### Lead 2: `api.sumup.com/authorize` client_id oracle + wildcard CORS + SameSite=None cookies

| # | Question | Answer |
|---|----------|--------|
| Q1 | In scope? | **YES** — `api.sumup.com` is company-owned API gateway. |
| Q2 | Attacker reachable? | **YES** — GET requests from unauthenticated context return 302 with error taxonomy. |
| Q3 | Real impact? | **LOW** — error taxonomy (`invalid_client` vs `invalid_request`) is standard OAuth2 error behavior per RFC 6749 §5.2. Wildcard CORS exists but the endpoint only redirects to error pages; no code/token leakage. SameSite=None cookie is `__cf_bm` (Cloudflare bot management), not session/auth. |
| Q4 | Provable non-invasively? | **YES** — GET with different `client_id` values shows different error classes. |
| Q5 | Novel/unreported? | **ALREADY REPORTED** — marked VALID in `valid-bugs.md` (2026-09-10). |
| Q6 | On always-rejected list? | Borderline — "Descriptive error messages" is out-of-scope, but this is OAuth error taxonomy (not stack traces). |
| Q7 | Reasonable triager? | **YES, but low bounty** — valid information disclosure, but standard OAuth errors. Already filed. |

**Verdict: INVALID (already reported + low impact)**
**Channel:** Already filed at bugs.olivermaicher.eu (VALID 2026-09-10, CVSS ~5.3)

---

### Lead 3: `mcp.sumup.com` wildcard CORS + Authorization allow-header on Bearer-protected MCP endpoint

| # | Question | Answer |
|---|----------|--------|
| Q1 | In scope? | **YES** — `mcp.sumup.com` is official SumUp MCP server (Cloudflare Worker). |
| Q2 | Attacker reachable? | **YES** — OPTIONS/GET requests are unauthenticated. |
| Q3 | Real impact? | **NO** — `bearer_methods_supported=["header"]`, no cookies/ambient credentials. Wildcard CORS is hardening-only; cannot exfiltrate bearer tokens cross-origin without JavaScript on the attacker's domain having access to the token (which it won't — tokens are set by the app, not by cookies). |
| Q4 | Provable non-invasively? | **YES** — OPTIONS request confirms CORS headers. |
| Q5 | Novel/unreported? | **ALREADY REPORTED** — marked VALID in `valid-bugs.md`. |
| Q6 | On always-rejected list? | Borderline — "Cookies that lack HTTP Only or Secure for non-sensitive data" is out-of-scope, but this is CORS not cookie config. |
| Q7 | Reasonable triager? | **YES, but already filed and impact is minimal.** |

**Verdict: INVALID (already reported + hardening-only)**
**Channel:** Already filed at bugs.olivermaicher.eu

---

### Lead 4: `help.sumup.com` Contentful Preview API token leak

| # | Question | Answer |
|---|----------|--------|
| Q1 | In scope? | **YES** — `help.sumup.com` is company-owned website. |
| Q2 | Attacker reachable? | **YES (was)** — token leaked in client-side `_app-*.js` chunk, accessible to any visitor. |
| Q3 | Real impact? | **YES** — grants unauthenticated read of 1,082 draft/unpublished entries (incl. 102 articles) in Contentful space. Pre-release content exposure. |
| Q4 | Provable non-invasively? | **YES (was)** — GET `preview.contentful.com/spaces/214q1nptnllb/entries` with leaked token returned 200 + entry data. |
| Q5 | Novel/unreported? | **YES at time of discovery** — marked VALID 2026-09-09. |
| Q6 | On always-rejected list? | **NO** |
| Q7 | Reasonable triager? | **YES, but token rotated 2026-09-11.** |

**Verdict: INVALID (closed — token rotated 2026-09-11, no longer reproducible)**
**Channel:** Was VALID at discovery; report should note token rotation.

---

### Lead 5: `api.sumup.com` BOLA via dashboard-client scopes (cross-merchant resource access)

| # | Question | Answer |
|---|----------|--------|
| Q1 | In scope? | **YES** — `api.sumup.com` is core payment API. |
| Q2 | Attacker reachable? | **NO** — all unauthenticated paths return 404. Requires valid merchant OAuth token. |
| Q3 | Real impact? | **YES (if confirmed)** — cross-merchant financial data (transactions, payouts, invoices). CRITICAL. |
| Q4 | Provable non-invasively? | **NO** — requires valid merchant OAuth token with dashboard scopes to test ID-swapping. AUTH_HELPED. |
| Q5 | Novel/unreported? | **Unproven** — hypothesis based on spec-documented oauth2:[] scopes, not demonstrated. |
| Q6 | On always-rejected list? | **NO** |
| Q7 | Reasonable triager? | **NO** — cannot accept a hypothesis without proof. Requires authenticated testing. |

**Verdict: HOLD**
**Reason:** High-potential but cannot be validated passively. Needs merchant OAuth token + cross-tenant ID manipulation proof.

---

### Lead 6: `api.sumup.com/.well-known/oauth-protected-resource` RFC 9728 metadata exposure

| # | Question | Answer |
|---|----------|--------|
| Q1 | In scope? | **YES** |
| Q2 | Attacker reachable? | **YES** — 200 JSON returned unauthenticated. |
| Q3 | Real impact? | **NO** — RFC 9728 resource-server metadata is designed to be public (tells clients where to get tokens). Declaring `authorization_servers`, `bearer_methods_supported`, `jwks_uri` is the spec's purpose. |
| Q4 | Provable non-invasively? | **YES** |
| Q5 | Novel/unreported? | **N/A** — spec-compliant behavior, not a finding. |
| Q6 | On always-rejected list? | **YES** — "Disclosure of known public files or directories" / informational metadata. |
| Q7 | Reasonable triager? | **NO** — standard RFC metadata endpoint, not a vulnerability. |

**Verdict: INVALID (spec-compliant public metadata, not a finding)**

---

### Lead 7: `api.sumup.com/token` legacy token endpoint routed with wildcard CORS + identity.svc header

| # | Question | Answer |
|---|----------|--------|
| Q1 | In scope? | **YES** |
| Q2 | Attacker reachable? | **YES** — OPTIONS returns 204 with CORS headers. |
| Q3 | Real impact? | **NO** — OPTIONS returns 204 (no content), wildcard CORS on a token endpoint that rejects all POST requests with `invalid_client` (no valid client credentials known). The `x-envoy-decorator-operation` header is out-of-scope (banner/stack trace leak). |
| Q4 | Provable non-invasively? | **YES** |
| Q5 | Novel/unreported? | **N/A** — the endpoint rejects all requests; no exploitable misconfiguration. |
| Q6 | On always-rejected list? | **YES** — "Descriptive error messages or headers" + "Descriptive error messages or headers (e.g. Stack Traces, banner grabbing)" are explicitly out-of-scope. |
| Q7 | Reasonable triager? | **NO** — routed but non-functional endpoint with out-of-scope header leak. |

**Verdict: INVALID (out-of-scope class + non-functional endpoint)**

---

### Lead 8: `portal.sumup.com` CNAME to `sumup.iriscrm.com` — third-party CRM supply-chain/SSRF surface

| # | Question | Answer |
|---|----------|--------|
| Q1 | In scope? | **UNCONFIRMED** — `portal.sumup.com` CNAME points to third-party domain (`sumup.iriscrm.com`). Program scope says "all company-owned infrastructure" — but a CNAME to a third-party CRM is not clearly company-owned infrastructure. |
| Q2 | Attacker reachable? | **PARTIALLY** — root returns 200 (React CRM login page), but webhook/callback parameter discovery requires active form interaction. |
| Q3 | Real impact? | **YES (if confirmed)** — SSRF to AWS metadata via webhook URL parameters. CRITICAL. |
| Q4 | Provable non-invasively? | **NO** — requires discovering webhook parameters via form interaction (active). |
| Q5 | Novel/unreported? | **Hypothesis only** — no parameter discovered. |
| Q6 | On always-rejected list? | **NO** |
| Q7 | Reasonable triager? | **NO** — third-party domain ownership unconfirmed, no PoC, requires active testing. |

**Verdict: HOLD (parked since 2026-09-03)**
**Reason:** Third-party domain scope unconfirmed; no passive proof possible; requires active parameter discovery.

---

### Lead 9: `chat.sumup.com/api/otel-traces` unauthenticated POST accepts arbitrary spans

| # | Question | Answer |
|---|----------|--------|
| Q1 | In scope? | **YES** — `chat.sumup.com` is company-owned (Vercel, official support chat). |
| Q2 | Attacker reachable? | **YES** — unauthenticated POST `{}` returns 200. |
| Q3 | Real impact? | **LOW** — OTEL collector accepting arbitrary spans is a shared marketing-widget template (per KB). Impact is limited to log pollution / trace injection. Not data exposure, not auth bypass. |
| Q4 | Provable non-invasively? | **YES** — POST with empty JSON body returns 200. |
| Q5 | Novel/unreported? | **Possibly novel** — not in `valid-bugs.md`. |
| Q6 | On always-rejected list? | Borderline — not explicitly listed, but "DoS" and "application-level attacks" might apply if trace volume can overwhelm backend. However, a single POST is not DoS. |
| Q7 | Reasonable triager? | **MAYBE** — depends on whether the triager considers arbitrary trace injection a security finding. Many programs reject "informational" OTEL/telemetry misconfigs. |

**Verdict: HOLD (pending assessment of triager tolerance)**
**Reason:** Novel but low impact; unauthenticated trace ingestion is debatable. Needs judgement call on whether log/trace pollution counts as "real security impact."

---

### Lead 10: `chat.sumup.com/api/conversations/[conversationId]` deterministic 500 on all IDs

| # | Question | Answer |
|---|----------|--------|
| Q1 | In scope? | **YES** |
| Q2 | Attacker reachable? | **YES** — GET returns 500 for any ID. |
| Q3 | Real impact? | **NO** — deterministic 500 (not 403/401) suggests the endpoint exists but auth enforcement is consistent. No data leak, no auth bypass. The 500 may be a bug, but "descriptive error messages" is out-of-scope. |
| Q4 | Provable non-invasively? | **YES** |
| Q5 | Novel/unreported? | **N/A** — not a security finding, just a bug. |
| Q6 | On always-rejected list? | **YES** — "Descriptive error messages or headers" is out-of-scope. |
| Q7 | Reasonable triager? | **NO** — error on unauthenticated request is not a security vulnerability. |

**Verdict: INVALID (out-of-scope: descriptive error messages)**

---

### Lead 11: `auth.sumup.com` OIDC discovery fully exposed (scope catalog, PAR, device flow, request_object alg=none)

| # | Question | Answer |
|---|----------|--------|
| Q1 | In scope? | **YES** |
| Q2 | Attacker reachable? | **YES** — `.well-known/openid-configuration` returns 200 unauthenticated. |
| Q3 | Real impact? | **NO** — OIDC discovery documents are **designed to be public** per OpenID Connect Core §8.1. Listing `request_object_signing_alg_values_supported=["none"]` is a capability declaration, not proof of acceptance. The discovery document tells clients what the server *supports*, not what it *allows for all flows*. redirect_uri strict allowlist already confirmed. |
| Q4 | Provable non-invasively? | **YES** |
| Q5 | Novel/unreported? | **N/A** — spec-compliant public metadata. |
| Q6 | On always-rejected list? | **YES** — "Disclosure of known public files or directories" + informational metadata. |
| Q7 | Reasonable triager? | **NO** — standard OIDC discovery, not a vulnerability. The `alg=none` in a discovery document is not the same as accepting unsigned request objects. |

**Verdict: INVALID (spec-compliant OIDC discovery, not a finding)**

---

### Lead 12: `chat.sumup.com` OAuth client `support_chat` — modern/legacy registry divergence

| # | Question | Answer |
|---|----------|--------|
| Q1 | In scope? | **YES** |
| Q2 | Attacker reachable? | **YES** — `support_chat` client_id is enumerable via the auth flow. |
| Q3 | Real impact? | **NO** — modern/legacy registry divergence is an internal deployment detail. `support_chat` is registered on modern auth server but not legacy gateway. This is expected behavior for a staged migration, not a vulnerability. No token/code leakage, no auth bypass. |
| Q4 | Provable non-invasively? | **YES** — `client_id=support_chat` returns `invalid_client` on legacy gateway vs `303 login_required` on modern. |
| Q5 | Novel/unreported? | **N/A** — not a security finding. |
| Q6 | On always-rejected list? | Borderline — informational, no security impact. |
| Q7 | Reasonable triager? | **NO** — deployment migration artifact, not a vulnerability. |

**Verdict: INVALID (internal deployment artifact, no security impact)**

---

### Lead 13: `mcp.sumup.com/.well-known/mcp.json` returns 404 JSON-RPC (not static file)

| # | Question | Answer |
|---|----------|--------|
| Q1 | In scope? | **YES** |
| Q2 | Attacker reachable? | **YES** — returns 404 with JSON-RPC structure. |
| Q3 | Real impact? | **NO** — 404 response with JSON-RPC error structure is normal for an MCP server that doesn't publish a static manifest. Not a data leak, not an auth bypass. |
| Q4 | Provable non-invasively? | **YES** |
| Q5 | Novel/unreported? | **N/A** |
| Q6 | On always-rejected list? | **YES** — "Disclosure of known public files or directories" / informational. |
| Q7 | Reasonable triager? | **NO** — 404 on a manifest endpoint is not a finding. |

**Verdict: INVALID**

---

## Summary Table

| # | Lead | Verdict | CVSS | Action |
|---|------|---------|------|--------|
| 1 | `auth.sam-app.ro` unauth dynamic client registration → JWTs | **VALID** | 7.5 | FILE REPORT |
| 2 | `api.sumup.com/authorize` client_id oracle + wildcard CORS | INVALID | 5.3 | Already reported |
| 3 | `mcp.sumup.com` wildcard CORS on Bearer endpoint | INVALID | 5.3 | Already reported + hardening-only |
| 4 | `help.sumup.com` Contentful Preview token leak | INVALID | — | Token rotated, closed |
| 5 | `api.sumup.com` BOLA via dashboard scopes | HOLD | — | Needs AUTH_HELPED proof |
| 6 | `api.sumup.com/.well-known/oauth-protected-resource` | INVALID | — | Spec-compliant metadata |
| 7 | `api.sumup.com/token` legacy endpoint + identity.svc header | INVALID | — | Out-of-scope class |
| 8 | `portal.sumup.com` third-party CRM SSRF hypothesis | HOLD | — | Scope unconfirmed, no PoC |
| 9 | `chat.sumup.com/api/otel-traces` unauth trace ingestion | HOLD | — | Low impact, debatable |
| 10 | `chat.sumup.com/api/conversations/[id]` deterministic 500 | INVALID | — | Out-of-scope error messages |
| 11 | `auth.sumup.com` OIDC discovery fully exposed | INVALID | — | Spec-compliant public metadata |
| 12 | `chat.sumup.com` support_chat modern/legacy divergence | INVALID | — | Deployment artifact |
| 13 | `mcp.sumup.com/.well-known/mcp.json` 404 | INVALID | — | Non-finding |

**Only 1 lead passes the gate: `auth.sam-app.ro` unauthenticated dynamic client registration (Lead 1).** This should be filed at bugs.olivermaicher.eu with PoC showing POST → 201 + token mint.
