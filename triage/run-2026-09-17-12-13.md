
===== TRIAGE 2026-09-17 12:13:01 UTC =====
[0m
> build · mimo-v2.5-free
[0m
[0m✱ [0mGlob "**/*"[90m 100 matches[0m
[0m→ [0mRead .
I notice the LEADS section is empty - no leads were provided for validation. Let me check the repository structure to understand the program context.
[0m→ [0mRead probe-results.md
[0m→ [0mRead probe-results.txt
[0m→ [0mRead scope.yml
[0m→ [0mRead inventory/sumup.md
[0m→ [0mRead leads
[0m→ [0mRead probe-results.md[90m [offset=411][0m
[0m→ [0mRead leads/lead-bigpickle.md
[0m→ [0mRead leads/lead-laguna.md
[0m→ [0mRead leads/lead-ling3.md
[0m→ [0mRead leads/lead-longcat.md
[0m→ [0mRead leads/lead-mimo.md
[0m→ [0mRead leads/lead-nemotron3.md
[0m→ [0mRead leads/reposcan-sumup.md
Now let me read the actual lead files produced by the hunt bots:
[0m→ [0mRead reports/valid-bugs.md
[0m→ [0mRead triage-output.txt[90m [limit=200][0m
[0m→ [0mRead knowledge/index.md
Let me check the latest triage output and valid bugs file:
Now I have the full picture. The LEADS section in your prompt was empty, so I extracted the distinct hypotheses from the hunt bot lead files and validated them against the 7-Question Gate using probe results and knowledge base as ground truth.

---

# TRIAGE — 7-Question Gate Validation

**Note:** LEADS were empty in the prompt. Extracted and deduplicated hypotheses from `leads/lead-bigpickle.md`, `leads/lead-nemotron3.md`, and `leads/reposcan-sumup.md`. Many are already triaged in `valid-bugs.md`. Below are the **distinct, not-yet-filed or re-evaluable** leads.

---

## LEAD 1: Contentful Preview API Token Leak (help.sumup.com)

| Q | Answer |
|---|--------|
| Q1 In scope? | YES — `help.sumup.com` is company-owned help center, covered by "all domains operated by SumUp Group" |
| Q2 Attacker reachable? | YES — public unauthenticated GET to help.sumup.com client-side JS leaks the Contentful Preview API token |
| Q3 Real impact? | YES — unauthenticated read of 1,082 draft/unpublished entries (incl. 102 articles) in space `214q1nptnllb`; pre-release content, product announcements, internal docs exposed |
| Q4 Provable non-invasively? | YES — GET `https://help.sumup.com` → extract token from client-side JS → `GET https://preview.contentful.com/spaces/214q1nptnllb/entries?access_token=<token>` returns 200 with draft entries. Read-only. |
| Q5 Novel/unreported? | PARTIALLY — previously marked VALID 2026-09-09, token rotated 2026-09-11. **Need to re-verify if token is still live**. If token was rotated and new token not leaked, this is closed. |
| Q6 Not on always-rejected list? | YES — not DoS, not deserial enumeration, not outdated lib, not scanner-only |
| Q7 Reasonable triager? | YES — but only if token is still live at submission time |

**Verdict: HOLD** — Previously valid and reportable (P4, CVSS 5.3). Token was rotated 2026-09-11 per valid-bugs.md. Must re-verify live token before submission. If token still leaks, VALID. If rotated and not re-exposed, INVALID (stale).

**If VALID:** Proof: `curl https://help.sumup.com | grep -o 'cdn.contentful.com[^"]*'` → extract token → `curl "https://preview.contentful.com/spaces/214q1nptnllb/entries?access_token=<TOKEN>&limit=1"` → 200 with draft data. Impact: pre-release content disclosure. CVSS 5.3 (Medium). Channel: bugs.olivermaicher.eu.

---

## LEAD 2: `auth.sam-app.ro` Unauthenticated RFC 7591 Dynamic Client Registration → Mintable JWTs

| Q | Answer |
|---|--------|
| Q1 In scope? | YES — `sam-app.ro` is confirmed SumUp staging infrastructure (discovered via sumup-mcp repo, confirmed live). Scope.yml covers "all company-owned infrastructure" |
| Q2 Attacker reachable? | YES — POST `https://auth.sam-app.ro/oauth2/register` is unauthenticated, returns 201 with client_id+secret |
| Q3 Real impact? | YES — attacker can register arbitrary OAuth clients, mint real JWT access tokens via client_credentials grant. JWTs are valid at api.sam-app.ro gateway (confirmed by structured error vs plain 404). Empty scope blocks resources but token is cryptographically valid |
| Q4 Provable non-invasively? | YES — POST to /oauth2/register with JSON body → 201 → POST to /oauth2/token with client_secret_post → 200 with JWT. Read-only (no data modification). |
| Q5 Novel/unreported? | YES — not in valid-bugs.md as filed report; staging-only but staging often leads to prod pivot |
| Q6 Not on always-rejected list? | YES — not DoS, not brute force, not info disclosure only |
| Q7 Reasonable triager? | YES — unauthenticated client registration on staging auth server with mintable tokens is a valid finding |

**Verdict: VALID**

**Proof steps (read-only):**
1. `POST https://auth.sam-app.ro/oauth2/register` with `{"redirect_uris":["https://evil.com"],"client_name":"test"}` → 201 with `client_id`, `client_secret`
2. `POST https://auth.sam-app.ro/oauth2/token` with `grant_type=client_credentials&client_id=<ID>&client_secret=<SECRET>&scope=` → 200 with JWT
3. `GET https://api.sumup.com/v1/merchants` with `Authorization: Bearer <JWT>` → structured error (confirms JWT validated)

**Impact:** Attacker-controlled OAuth clients on staging; mintable JWTs valid at API gateway. Staging environments often lack prod hardening. Potential pivot to production if shared credentials/configs.
**CVSS 7.5** (High) — NETWORK/LOW/NONE/CHANGED/LOW/NONE
**Channel:** bugs.olivermaicher.eu

---

## LEAD 3: `api.sumup.com/authorize` Client-ID Oracle + Wildcard CORS

| Q | Answer |
|---|--------|
| Q1 In scope? | YES — `api.sumup.com` is core payment API |
| Q2 Attacker reachable? | YES — GET `https://api.sumup.com/authorize?client_id=test` is unauthenticated, returns 302 to error flow |
| Q3 Real impact? | LOW — error taxonomy (`invalid_client` vs `invalid_request`) is standard OAuth error behavior. Wildcard CORS exists but the endpoint only redirects to error pages; no code/token leakage. Already marked VALID in valid-bugs.md as information disclosure. |
| Q4 Provable non-invasively? | YES — GET requests with different client_ids |
| Q5 Novel/unreported? | ALREADY REPORTED — marked VALID in valid-bugs.md (2026-09-10) |
| Q6 Not on always-rejected list? | YES |
| Q7 Reasonable triager? | YES but already filed |

**Verdict: INVALID (duplicate)** — Already triaged and marked VALID in valid-bugs.md. No new angle.

---

## LEAD 4: `mcp.sumup.com` Wildcard CORS on Bearer-Protected Endpoint

| Q | Answer |
|---|--------|
| Q1 In scope? | YES — `mcp.sumup.com` is official SumUp MCP server |
| Q2 Attacker reachable? | YES — public endpoint |
| Q3 Real impact? | LOW — wildcard CORS + `Authorization: Bearer` header-only auth. No cookies ambient creds. Hardening-only config. Already marked VALID in valid-bugs.md. |
| Q4 Provable non-invasively? | YES — `curl -I -X OPTIONS` |
| Q5 Novel/unreported? | ALREADY REPORTED |
| Q6 Not on always-rejected list? | YES |
| Q7 Reasonable triager? | Already filed |

**Verdict: INVALID (duplicate)** — Already triaged and marked VALID in valid-bugs.md.

---

## LEAD 5: API BOLA via Dashboard-Client Scopes (api.sumup.com)

| Q | Answer |
|---|--------|
| Q1 In scope? | YES — api.sumup.com |
| Q2 Attacker reachable? | Partially — all unauthenticated paths return 404. Requires valid merchant OAuth token. |
| Q3 Real impact? | YES (if exploitable) — cross-merchant financial/PII data exposure |
| Q4 Provable non-invasively? | **NO** — requires valid merchant OAuth token with dashboard scopes. All unauthenticated paths 404. Cannot test passively. |
| Q5 Novel/unreported? | Hypothesis only — no proof of vulnerability exists |
| Q6 Not on always-rejected list? | YES |
| Q7 Reasonable triager? | NO — scanner-output-level hypothesis without proof. Program rules require PoC. |

**Verdict: INVALID** — Requires AUTH_HELPED (merchant OAuth token) which is not available. No passive proof possible. Program rejects "automated scanning utilities without additional details or a POC."

---

## LEAD 6: OAuth `token_endpoint_auth_methods=none` Public Client Impersonation (auth.sumup.com)

| Q | Answer |
|---|--------|
| Q1 In scope? | YES — auth.sumup.com |
| Q2 Attacker reachable? | Partially — OIDC discovery is public, but actual token flow requires valid client registration |
| Q3 Real impact? | YES (if exploitable) — client impersonation → merchant account takeover |
| Q4 Provable non-invasively? | **NO** — PAR endpoint returns 404 on OPTIONS (unrouted at gateway). Dashboard client rejects unauthenticated token requests. Need AUTH_HELPED to test full flow. |
| Q5 Novel/unreported? | Documented in OIDC discovery but exploitability unproven. redirect_uri is strictly allowlisted per client. |
| Q6 Not on always-rejected list? | YES |
| Q7 Reasonable triager? | BORDERLINE — documented OAuth setting + potential vector, but no proof of exploitability and multiple mitigations in place (strict redirect_uri allowlist, PAR unrouted) |

**Verdict: HOLD** — Interesting theoretical vector but insufficient proof. redirect_uri strict allowlist + PAR unrouted + no public client registration = mitigated. Could be VALID if a public client registration path is found. Needs active testing with auth assistance.

---

## LEAD 7: `me.sumup.com` Vercel Merchant Self-Service Surface

| Q | Answer |
|---|--------|
| Q1 In scope? | YES — me.sumup.com is merchant self-service portal |
| Q2 Attacker reachable? | Partially — anonymous /api/* routes return 307/403 to OAuth flow. No debug endpoints found passively. |
| Q3 Real impact? | Potential — but no vulnerability found. All anonymous routes gated. |
| Q4 Provable non-invasively? | NO — all anonymous routes redirect to OAuth. Full test requires authenticated session (AUTH_HELPED). |
| Q5 Novel/unreported? | No finding to report — hypothesis only |
| Q6 Not on always-rejected list? | YES |
| Q7 Reasonable triager? | NO — no finding, only hypothesis. Scanner-output-level. |

**Verdict: INVALID** — No vulnerability found. All anonymous routes properly gated. Hypothesis without proof.

---

## LEAD 8: `admin.sumup.com` Internal Admin Panel Auth Bypass

| Q | Answer |
|---|--------|
| Q1 In scope? | YES — admin.sumup.com |
| Q2 Attacker reachable? | Partially — returns 403 consistently |
| Q3 Real impact? | YES (if exploitable) — but all header spoofing attempts yield identical 403 |
| Q4 Provable non-invasively? | NO — header manipulation (Host, X-Forwarded-For, X-Original-URL) yields no status change |
| Q5 Novel/unreported? | No finding — hardened |
| Q6 Not on always-rejected list? | YES |
| Q7 Reasonable triager? | NO — no proof after exhaustive passive testing |

**Verdict: INVALID** — Exhaustive passive header spoofing yielded no auth bypass. 403 is consistent and correct.

---

## LEAD 9: `help.sumup.com/api/search` HTTP 500 Error Disclosure

| Q | Answer |
|---|--------|
| Q1 In scope? | YES |
| Q2 Attacker reachable? | YES — unauthenticated GET |
| Q3 Real impact? | NO — deterministic 500 is missing-required-params throw (page/size/locale). No draft leak via search. Already REJECTED in knowledge base. |
| Q4 Provable non-invasively? | YES but no impact |
| Q5 Novel/unreported? | Already rejected |
| Q6 Not on always-rejected list? | Descriptive error messages / stack traces are explicit out-of-scope |
| Q7 Reasonable triager? | NO — explicit out-of-scope class |

**Verdict: INVALID** — Descriptive error messages are out-of-scope per program rules.

---

## LEAD 10: RepoScan — Terraform Provider Private Key Logging (terraform-provider-kafka-connect)

| Q | Answer |
|---|--------|
| Q1 In scope? | BORDERLINE — open-source Terraform provider fork, not a live SumUp production service |
| Q2 Attacker reachable? | NO — requires deploying Terraform with verbose logging. Not remotely accessible. |
| Q3 Real impact? | Code-level issue in provider.go:88 logging cert+key. But this is a Terraform provider, not a production service. |
| Q4 Provable non-invasively? | YES — static code review of public repo |
| Q5 Novel/unreported? | Yes in this fork, but this is a development tool issue |
| Q6 Not on always-rejected list? | YES |
| Q7 Reasonable triager? | BORDERLINE — private key logging in a Terraform provider is a real code issue, but it's a development tool, not a live production service. Likely informational at best. |

**Verdict: HOLD** — Valid code issue (private key material in log output) but in a development tool, not production infrastructure. May be accepted as informational. Needs scoping clarification.

---

## Summary

| # | Lead | Verdict | CVSS | Action |
|---|------|---------|------|--------|
| 1 | Contentful Preview Token Leak (help.sumup.com) | **HOLD** | 5.3 | Re-verify token liveness; if live → VALID, FILE REPORT |
| 2 | auth.sam-app.ro unauthenticated client registration → JWTs | **VALID** | 7.5 | FILE REPORT |
| 3 | api.sumup.com/authorize client_id oracle | **INVALID** (dup) | — | Already filed |
| 4 | mcp.sumup.com wildcard CORS | **INVALID** (dup) | — | Already filed |
| 5 | API BOLA via dashboard scopes | **INVALID** | — | No proof, needs AUTH_HELPED |
| 6 | OAuth token_endpoint_auth_methods=none impersonation | **HOLD** | — | Mitigated; needs active testing |
| 7 | me.sumup.com Vercel surface | **INVALID** | — | No finding |
| 8 | admin.sumup.com auth bypass | **INVALID** | — | Hardened |
| 9 | help.sumup.com/api/search 500 | **INVALID** | — | Out-of-scope class |
| 10 | Terraform provider key logging | **HOLD** | — | Dev tool, not prod |

**Recommended immediate actions:**
1. Re-verify `help.sumup.com` Contentful token liveness — if live, file report
2. File `auth.sam-app.ro` dynamic registration → JWT finding (CVSS 7.5)
3. Park BOLA + OAuth impersonation hypotheses until AUTH_HELPED testing is possible
