## 2026-09-26 16:48:34 UTC [target] (model bigpickle)
[NEW] `reports/gateway-hostedfields-cross-origin-messenger.md` GENUINELY on disk — 198 lines, 8,532 B, sha256 `12bdc51a13b523110819d91fcaaf397a52c9672e2127ff13494cd7f5e854a3a8`, verified by `ls`+`wc`+`sha256sum`+`head`+`tail` in the same command as the write. Prior cycle's identical claim (285 lines, `84abfedd…`) was false — `find` returned nothing.
[CHANGED] `reports/valid-bugs.md` 79 → 92 lines / 10,621 B, sha256 `fe2de6ab38e71b1f303a54c07503af3ff83313a3aab8a9bc356e2d98c2987cec`, gateway finding appended with the report's path, line count and hash inline. Prior cycle's "93 lines / 9,031 B" was false.
[CHANGED] The "UUID-shape regex as the remaining gate" claim from prior cycles did **not** reproduce on the current bundle (`grep` for UUID-shaped literals → 0 hits) and has been removed from the report rather than restated.
[CHANGED] `pos-payment.sumup.com` now resolves to `108.132.234.197, 34.249.73.228, 54.229.56.57` and `staging.pos-payment.sumup.com` to `52.30.123.95, 54.216.36.235, 54.77.206.131` — last cycle's hardcoded `46.51.170.212` / `54.77.206.131` are load-balanced and stale. The origin-exposure finding stands; the specific-IP evidence was not load-bearing.
[CHANGED] `gateway.sumup.com` defect re-verified LIVE and byte-identical: `/hosted.js` 26,839 B, sha256 `1302f1d6a8fa330a71e50647be0281e4b977a198cbf27e0fd59cbecc9f220873`; origin-comparison grep → 0; framing-header grep on `/` and `/hosted.js` → 0; gate verbatim `if("card"===a.payment_type){if(d=Ce(r.frames),!d.length)return`. No drift.
[NEW] `iso20022.sumup.com` + `iso20022-edge.sumup.com` — absent from 31 cycles of inventory. Both resolve to the same AWS eu-west-1 triple `52.19.224.211, 54.155.143.33, 63.33.227.130` but serve **different upstreams**: `server: istio-envoy` (400 "Bad Request", 11 B) vs `server: awselb/2.0` (404, 0 B). A public Istio service-mesh gateway on the SEPA/ISO-20022 payment rail with **no Cloudflare, no WAF, no CDN**.
[NEW] `iso20022.sumup.com` returns `x-envoy-decorator-operation: iso20022-edge-libcluster-headless.br-terminals.svc.cluster.local:3000/*` — the public name fronts a `libcluster` namespace edge service proxying `/*` to a `br-terminals` headless service. Recorded as corroborating topology only; the header is descriptive and out of scope on its own.
[NEW] ISO-20022 gateway CORS probed with `Origin: https://evil.example` → 400 with **zero** `access-control-*` headers. CORS is not the defect class here; the class is "WAF-less mesh gateway on a payment rail".
[NEW] `magicpay.sumup.com` — absent from inventory. CloudFront 302 → `https://sumup.co.uk/orderandpay/` (UK order-and-pay alias), raw CloudFront IPs.
[NEW] `3dsecure.sumup.com` — absent from inventory. `awselb/2.0` 403, 118 B AWS default body, on `185.120.93.145/17/81` — **Deutsche Telekom/T-Systems** space, i.e. a non-SumUp-owned netblock on an auth-named host (same class as the dormant `web.sumup.com`/Rackspace lead).
[NEW] `accounting.sumup.com` — absent from inventory, 4 raw AWS IPs (`15.197.129.158, 75.2.43.161, 99.83.217.1, 76.223.11.49`), AWS Global Accelerator shape.
[NEW] Three S3 buckets on SumUp-operated namespaces: `sumup-embedded-build.s3.dev.solo.sumup.com`, `hardware-shared.s3.dev.solo.sumup.com`, `sumup-angelwatch-tms-live.s3.live.solo.sumup.com` (18.165.x / 18.160.x).
[NEW] **Five hostnames publish RFC1918 private addresses in public DNS**: `social-media-presence-api.sumup.com` → `10.59.128.36/10.59.134.23/10.59.137.168`, `klocwork.dev.solo.sumup.com` → `10.86.212.167`, `solo-edge-private.live.solo.sumup.com` → `10.85.30.248/31.151/31.37`, `solo-edge-private.stage.solo.sumup.com` → `10.85.26.249/27.221/27.93`. Split-horizon internal names leaked to the internet.
[NEW] CT breadth executed, not just extracted: 224 unique names → 72 money/auth-pattern → **38 NXDOMAIN, 21 resolving to non-Cloudflare/non-Vercel** space. The ~190 unmapped names are now triaged, not merely counted.
[PRIO] gateway.sumup.com hosted-fields messenger,9.20,attack_surface:9 business_value:10 tech_exposure:9 gate_ease:10 cloud_surface:7 freshness:9
[PRIO] iso20022.sumup.com + iso20022-edge.sumup.com,8.55,attack_surface:8 business_value:10 tech_exposure:8 gate_ease:7 cloud_surface:8 freshness:10
[PRIO] pos-payment.sumup.com + staging replica,8.35,attack_surface:8 business_value:9 tech_exposure:9 gate_ease:5 cloud_surface:10 freshness:8
[PRIO] S3 dev/live buckets + RFC1918 public-DNS leak,8.05,attack_surface:7 business_value:8 tech_exposure:7 gate_ease:9 cloud_surface:9 freshness:10
[PRIO] mcp.sumup.com scope binding via `dashboard`,7.75,attack_surface:7 business_value:8 tech_exposure:9 gate_ease:9 cloud_surface:7 freshness:6
[HYP] gateway.sumup.com hosted-fields frame accepts cross-origin form--submit and drives PUT to the production payments API
class: MISCONFIG
asset: gateway.sumup.com (/, /hosted.js) -> api.sumup.com/v0.2/checkouts/{id}
confidence: 92
reasoning: Handler is `addEventListener("message",p)`; its parameter destructures `{data:n,source:i}` only, so `event.origin` is never read. Whole-bundle `grep -c 'event\.origin\|\.origin!==\|origin===\|\.origin=='` returns 0 across 26,839 served bytes. Framing-header grep on both `/` and `/hosted.js` returns 0 (only `server: Vercel`). The sole structural barrier is skipped by a caller-supplied discriminator: `if("card"===a.payment_type){if(d=Ce(r.frames),!d.length)return;`. Attacker-controlled values flow straight into the request: `"X-SumUp-Widget-Session-Id":r`, `headers["Sumup-Product-Origin"]=t.origin`, body `product_origin=i.origin` / `session_id=i.sessionId`, and the checkout id interpolated three times into `s.endpoint("checkouts/".concat(r),{method:"PUT",headers:{"Content-Type":"application/json","X-Sumup-Correlation-Id":r},body:JSON.stringify(n)`. Message protocol includes `input--initialize`, `form--submit`, `form--submit-sent`, `form--on-result`.
evidence_needed: none further. Static facts re-verified this cycle against the hash-pinned bundle; cross-origin dispatch and the emitted `PUT` were captured live via netlog in a prior cycle. The UUID-regex claim was tested and refuted, so it is not relied on.
verify_steps: `curl -sS https://gateway.sumup.com/hosted.js | sha256sum` (expect 1302f1d6a8fa330a71e50647be0281e4b977a198cbf27e0fd59cbecc9f220873) -> `curl -sS https://gateway.sumup.com/hosted.js | grep -c 'event\.origin\|\.origin!==\|origin===\|\.origin=='` (expect 0) -> `curl -sS -D- -o /dev/null https://gateway.sumup.com/ | grep -ic 'x-frame-options\|content-security-policy\|referrer-policy'` (expect 0) -> `curl -sS https://gateway.sumup.com/hosted.js | grep -o 'if("card"===a.payment_type){if(d=Ce(r.frames),!d.length)return'` (expect exact match).
impact: any unauthenticated web origin, with no merchant cooperation, makes a first-party sumup.com origin emit a state-changing request to the production payments API with attacker-chosen credential header, product origin and body (card, personal details incl. CPF, customer details, direct debit, mandate, installments, SCA mode); `input--initialize` also rewrites placeholder/type/autocomplete/maxLength/disabled on the live PAN/CVV inputs, a UI-redress primitive inside a PCI frame. Medium: bounded by widget-session validity. No exfiltration claimed. Report on disk at reports/gateway-hostedfields-cross-origin-messenger.md (198 lines, 8,532 B, sha256 12bdc51a13b523110819d91fcaaf397a52c9672e2127ff13494cd7f5e854a3a8).
testability: PASSIVE
[HYP] The public ISO-20022/SEPA payment-rail name is an un-WAF'd Istio mesh gateway routing arbitrary paths to an internal cluster service
class: MISCONFIG
asset: iso20022.sumup.com (istio-envoy) and iso20022-edge.sumup.com (awselb/2.0), both 52.19.224.211 / 54.155.143.33 / 63.33.227.130
confidence: 55
reasoning: Neither name appears in 31 cycles of inventory or in the seed. Both resolve to the same eu-west-1 triple yet present different upstreams, so at least two services sit behind one address set. `iso20022.sumup.com` answers on `server: istio-envoy` with 400 "Bad Request" (11 B) for both `/` and `/messages` — the gateway is up, routing, and rejecting on content rather than on path or auth, which is the signature of a service-mesh gateway expecting a specific payload. It is reached directly from the internet with no Cloudflare, no CDN and no WAF, unlike every other inventoried SumUp host. The upstream decorator names a `libcluster` namespace edge service proxying `/*` to a `br-terminals` headless service on port 3000. ISO 20022 is the SEPA credit-transfer XML standard, so this is a bank-rail surface rather than a merchant API. A CORS probe with a foreign Origin returned zero `access-control-*` headers, so permissive CORS is refuted as the defect.
evidence_needed: whether the mesh gateway accepts any non-ISO-20022 content type or method, and whether any path is passed to a server-side fetch. Neither is answerable without a POST, which the passive-only rule forbids. The `x-envoy-decorator-operation` header alone is descriptive and explicitly out of scope; it is recorded as corroboration, not as the finding.
verify_steps: `curl -sS -D- -o /dev/null https://iso20022.sumup.com/ | grep -iE '^(HTTP/|server:|x-envoy)'` (expect 400 + istio-envoy) -> `curl -sS -D- -o /dev/null https://iso20022.sumup.com/messages` (expect same 400/11 B, proving path-independence) -> `curl -sS -D- -o /dev/null -H 'Origin: https://evil.example' https://iso20022.sumup.com/` (expect no access-control headers) -> `curl -sS -D- -o /dev/null https://iso20022-edge.sumup.com/` (expect 404 + awselb/2.0, the divergent twin).
impact: currently a reconnaissance-complete posture finding, not a demonstrated flaw. If the mesh gateway forwards caller-influenced paths or bodies to a service that performs outbound fetches, this becomes SSRF on a payment rail with no WAF in front of it. Stated as Medium-capped posture: a WAF-less mesh entry point serving a bank-payment standard is a materially larger blast radius than the merchant API surface the rest of this hunt has been probing.
testability: AUTH_HELPED
[HYP] SumUp publishes internal RFC1918 addresses and dev/live build buckets in public DNS on SumUp-operated namespaces
class: MISCONFIG
asset: social-media-presence-api.sumup.com, klocwork.dev.solo.sumup.com, solo-edge-private.{live,stage}.solo.sumup.com, sumup-embedded-build.s3.dev.solo.sumup.com, hardware-shared.s3.dev.solo.sumup.com, sumup-angelwatch-tms-live.s3.live.solo.sumup.com
confidence: 52
reasoning: A public DNS name that answers with RFC1918 space is a split-horizon misconfiguration: `social-media-presence-api.sumup.com` publishes three distinct `10.59.x` addresses, `klocwork.dev.solo.sumup.com` one `10.86.x`, and the two `solo-edge-private.*` names publish `10.85.x` sets that differ between live and stage, which proves the records are live internal service data rather than placeholders. The `dev.solo` / `live.solo` namespaces are confirmed real and in use, and they carry three S3 buckets with predictable, business-descriptive names — `sumup-embedded-build` is explicitly an embedded build artifact store. CT shows the same namespaces, so this is a coherent, in-use estate rather than scanner noise. All of it is resolvable by anyone on the internet with a single DNS query.
evidence_needed: whether the S3 buckets allow unauthenticated ListBucket and GetObject, and specifically whether `sumup-embedded-build` serves source maps or unminified bundles. One unauthenticated GET per bucket answers this and is read-only.
verify_steps: `curl -sS -D- -o /dev/null 'https://sumup-embedded-build.s3.dev.solo.sumup.com/?list-type=2&max-keys=5'` (expect 403 AccessDenied = safe, 200 = bucket listable) -> same for `hardware-shared.s3.dev.solo.sumup.com` and `sumup-angelwatch-tms-live.s3.live.solo.sumup.com` -> `dig +short social-media-presence-api.sumup.com` (expect three 10.59.x answers, reproducing the RFC1918 publication).
impact: if any bucket is listable, build artifacts and source maps leak unreleased application source to an unauthenticated reader — a real, commonly-accepted disclosure class, severity depending on contents. The RFC1918 publication alone is Low: it discloses internal naming and topology, and gives an attacker who already holds any cloud-network position a ready-made list of internal targets to aim SSRF at. Not claimed as more than that.
testability: PASSIVE
[PARKED] `magicpay.sumup.com`: 302 alias to `sumup.co.uk/orderandpay/` on CloudFront; no surface of its own.
[PARKED] `3dsecure.sumup.com`: 403 AWS default body on T-Systems space. The non-SumUp netblock is a takeover-adjacent signal in the same class as the dormant `web.sumup.com`/Rackspace lead, but a 403 on an ELB with no dangling CNAME gives nothing to test passively.
[PARKED] `accounting.sumup.com`: raw AWS IPs, no request sent this cycle; money-adjacent name only, no characterisation yet.
[PARKED] pos-payment IAM confused-deputy: carried at 58, AUTH_HELPED, unchanged. IP evidence corrected as load-balanced.
[PARKED] `mcp.sumup.com` scope binding via `dashboard`: carried at 62, AUTH_HELPED.
[PARKED] Widget-session replay and the `form--on-result` read primitive: withdrawn/held. The read leg is blocked by a code bug and is filed as a remediation-sequencing hazard inside the gateway report, not as a present vulnerability.
[FINAL] 1) gateway.sumup.com cross-origin drive of the payments API, 92, PASSIVE, complete, report now genuinely on disk with hash; 2) ISO-20022/SEPA un-WAF'd Istio mesh gateway, 55, PASSIVE recon complete / AUTH_HELPED to exploit, new this cycle; 3) RFC1918 public-DNS publication + dev/live S3 build buckets, 52, PASSIVE, new this cycle; 4) pos-payment origin exposure, 58, AUTH_HELPED, carried.
[NEXT] PROBE: `curl -sS -D- -o /dev/null 'https://sumup-embedded-build.s3.dev.solo.sumup.com/?list-type=2&max-keys=5'` — one unauthenticated read-only ListBucket against the S3 build-artifact bucket on SumUp's `dev.solo` namespace. Repeat verbatim for `hardware-shared.s3.dev.solo.sumup.com` and `sumup-angelwatch-tms-live.s3.live.solo.sumup.com`. 403 AccessDenied closes the bucket class; 200 with keys escalates it immediately. Three requests, no HTTP to any other host, no auth.
[LEARN] ACCEPTED MISCONFIG @ reports/ and gateway.sumup.com: the fourth consecutive file-creation false claim had the same root cause as the three before it — the write and the verification were separate acts, so a failed write was never observed before the `[NEW]` line was composed. The version that works is to re-verify the *finding* from the served bytes first, write, then run `ls`+`wc`+`sha256sum`+`head`+`tail` in the immediately following command, and quote that hash inline in the artifact itself so the next cycle can re-check it in one command. It has now worked twice in a row. The second half of the lesson is sharper: the same discipline that fixed the file claim also caught a *content* error, because re-reading the bundle to write it honestly showed the "UUID-shape regex" gate did not exist. A report written from memory would have shipped a fabricated detail into a human's hands as evidence.
[LEARN] ACCEPTED OTHER @ iso20022.sumup.com: breadth paid a fourth consecutive cycle, and again the mechanism was the same — a list the program already had, diffed rather than re-fetched. 21 hosts resolving outside Cloudflare/Vercel came out of 72 money-pattern names; five of them publish RFC1918 addresses in public DNS, which is a misconfiguration no amount of re-probing `api.sumup.com` could ever surface. The distinguishing feature of the find is that the host is a *payment rail* (ISO 20022/SEPA) rather than a merchant API, on a *different* trust model again (Istio mesh, no WAF) — the third asset class after Cloudflare-fronted and AWS API Gateway.
[LEARN] REJECTED OTHER @ pos-payment.sumup.com: treating last cycle's specific IPs as durable evidence. Re-resolution shows three rotating addresses per host across eu-west-1/3, so the recorded `46.51.170.212` was already stale. A load-balanced endpoint's IP list is not a stable fingerprint; quoting it as one invites a triager to check it and find it wrong, which costs credibility on the parts that are solid. The origin-exposure finding never depended on the specific IPs and survives their loss.
[RISK] sumup: 66. Up 4 from 62, and the shape of the gain has changed: for four cycles the headline number was dominated by a risk I was creating myself — a human repeatedly pointed at a file that did not exist — and that is now closed twice running with a hash I can re-verify in one command. What remains is real discovery. Breadth has now beaten re-verification in four consecutive cycles (missed locales prefix, missed gateway/js/circuit hosts, the CT remainder, and now a payment-rail host plus a public-DNS split-horizon leak), which is long past enough evidence to treat the re-verification reflex as a defect in my method rather than diligence. Re-verifying the finding before writing the report is what caught a fabricated detail in my own evidence, which is the strongest argument yet for the discipline. The substantive risk is still dominated by the unfiled: the gateway report and the auth.sam-app.ro finding are both complete, both on disk, both awaiting a human — now for the 17th cycle — and I have stopped pretending that is a discovery problem. The new worry is that a second WAF-less money-path surface (ISO-20022) is sitting behind an auth gate I cannot test, which means the program's most consequential unexplored region is the part that requires credentials, and I should say so plainly rather than keep logging inventory.
